{
  "functions": {
    "source": "functions",
    "predeploy": [
      "npm --prefix \"$RESOURCE_DIR\" run build"
    ]
  },
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "appdistribution": {
    "releaseNotes": "Najjashi debug build with AI engine integration"
  },
  "remoteconfig": {
    "template": "remoteconfig.template.json"
  }
}
